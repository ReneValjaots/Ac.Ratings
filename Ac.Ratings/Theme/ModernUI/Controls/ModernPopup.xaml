<Popup
    x:Class="Ac.Ratings.Theme.ModernUI.Controls.ModernPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Ac.Ratings.Theme.ModernUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    HorizontalAlignment="Left"
    AllowsTransparency="True"
    Placement="Custom"
    PopupAnimation="{DynamicResource {x:Static SystemParameters.MenuPopupAnimationKey}}"
    StaysOpen="False"
    mc:Ignorable="d">
    <Popup.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Theme/ModernUI/Assets/Button.xaml" />
                <ResourceDictionary Source="/Theme/ModernUI/Assets/Converters.xaml" />
                <ResourceDictionary Source="/Theme/ModernUI/Assets/TextBlock.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Popup.Resources>

    <controls:Cell
        x:Name="Root"
        Margin="0"
        HorizontalAlignment="Center"
        d:DataContext="{d:DesignInstance controls:ModernPopup}"
        TextBlock.FontSize="{DynamicResource DefaultFontSize}">
        <Border Padding="{Binding Padding}" Background="{DynamicResource PopupBackground}">
            <AdornerDecorator>
                <ContentPresenter
                    Content="{Binding Content}"
                    TextBlock.FontStyle="Normal"
                    TextBlock.FontWeight="Normal"
                    TextOptions.TextFormattingMode="{DynamicResource FormattingMode}" />
            </AdornerDecorator>
        </Border>
        <Border BorderBrush="{DynamicResource WindowBorderActive}" BorderThickness="1" />
    </controls:Cell>
</Popup>