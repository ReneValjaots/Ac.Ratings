<UserControl x:Class="Ac.Ratings.View.Controls.ExtraFeaturesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="10" Orientation="Vertical">
        <TextBlock Margin="0,0,0,10"
                   HorizontalAlignment="Center"
                   FontSize="16"
                   FontWeight="Bold"
                   Text="Extra Features" />

        <TextBlock Margin="0,5"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Text="Dashboard Lights:" />
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Content="Turn Signals" />
            <CheckBox Grid.Row="0" Grid.Column="1" Margin="5" IsChecked="{Binding Ratings.TurnSignalsDashboard, Mode=TwoWay}" />
            <Label Grid.Row="0" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" Content="ABS On (Flashing)" />
            <CheckBox Grid.Row="0" Grid.Column="3" Margin="5" IsChecked="{Binding Ratings.AbsOnFlashing, Mode=TwoWay}" />

            <Label Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Content="TC On (Flashing)" />
            <CheckBox Grid.Row="1" Grid.Column="1" Margin="5" IsChecked="{Binding Ratings.TcOnFlashing, Mode=TwoWay}" />
            <Label Grid.Row="1" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" Content="ABS Off" />
            <CheckBox Grid.Row="1" Grid.Column="3" Margin="5" IsChecked="{Binding Ratings.AbsOff, Mode=TwoWay}" />

            <Label Grid.Row="2" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Content="TC Off" />
            <CheckBox Grid.Row="2" Grid.Column="1" Margin="5" IsChecked="{Binding Ratings.TcOff, Mode=TwoWay}" />
            <Label Grid.Row="2" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" Content="Handbrake" />
            <CheckBox Grid.Row="2" Grid.Column="3" Margin="5" IsChecked="{Binding Ratings.Handbrake, Mode=TwoWay}" />

            <Label Grid.Row="3" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Content="Lights" />
            <CheckBox Grid.Row="3" Grid.Column="1" Margin="5" IsChecked="{Binding Ratings.LightsDashboard, Mode=TwoWay}" />
            <Label Grid.Row="3" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" Content="Other" />
            <CheckBox Grid.Row="3" Grid.Column="3" Margin="5" IsChecked="{Binding Ratings.OtherDashboard, Mode=TwoWay}" />
        </Grid>

        <TextBlock Margin="0,5"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Text="Exterior:" />
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Content="Turn Signals" />
            <CheckBox Grid.Row="0" Grid.Column="1" Margin="5" IsChecked="{Binding Ratings.TurnSignalsExterior, Mode=TwoWay}" />
            <Label Grid.Row="0" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" Content="Good Quality Lights" />
            <CheckBox Grid.Row="0" Grid.Column="3" Margin="5" IsChecked="{Binding Ratings.GoodQualityLights, Mode=TwoWay}" />

            <Label Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Content="Emergency Brake Lights" />
            <CheckBox Grid.Row="1" Grid.Column="1" Margin="5" IsChecked="{Binding Ratings.EmergencyBrakeLights, Mode=TwoWay}" />
            <Label Grid.Row="1" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" Content="Fog Lights" />
            <CheckBox Grid.Row="1" Grid.Column="3" Margin="5" IsChecked="{Binding Ratings.FogLights, Mode=TwoWay}" />

            <Label Grid.Row="2" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Content="Sequential Turn Signals" />
            <CheckBox Grid.Row="2" Grid.Column="1" Margin="5" IsChecked="{Binding Ratings.SequentialTurnSignals, Mode=TwoWay}" />
            <Label Grid.Row="2" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" Content="Animations" />
            <CheckBox Grid.Row="2" Grid.Column="3" Margin="5" IsChecked="{Binding Ratings.Animations, Mode=TwoWay}" />
        </Grid>

        <TextBlock Margin="0,5"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Text="Others:" />
        <Grid Margin="0,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Content="Extended Physics" />
            <CheckBox Grid.Row="0" Grid.Column="1" Margin="5" IsChecked="{Binding Ratings.ExtendedPhysics, Mode=TwoWay}" />
            <Label Grid.Row="0" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" Content="Startup Sound" />
            <CheckBox Grid.Row="0" Grid.Column="3" Margin="5" IsChecked="{Binding Ratings.StartupSound, Mode=TwoWay}" />

            <Label Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Content="Different Displays" />
            <CheckBox Grid.Row="1" Grid.Column="1" Margin="5" IsChecked="{Binding Ratings.DifferentDisplays, Mode=TwoWay}" />
            <Label Grid.Row="1" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" Content="Different Driving Modes" />
            <CheckBox Grid.Row="1" Grid.Column="3" Margin="5" IsChecked="{Binding Ratings.DifferentDrivingModes, Mode=TwoWay}" />
        </Grid>
    </StackPanel>
</UserControl>
