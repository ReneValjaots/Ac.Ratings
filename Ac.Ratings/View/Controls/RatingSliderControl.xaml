<UserControl
    x:Class="Ac.Ratings.View.Controls.RatingSliderControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Margin="0,5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock
            Grid.Column="0"
            Margin="0,0,10,0"
            VerticalAlignment="Center">
            <TextBlock.Text>
                <MultiBinding StringFormat="{}{0}: {1}">
                    <Binding Path="LabelText" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                    <Binding Path="SliderValue" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>
        <Slider
            Grid.Column="1"
            Width="{Binding SliderWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"
            IsSnapToTickEnabled="True"
            IsMoveToPointEnabled="True"
            Maximum="10"
            Minimum="0"
            Style="{Binding SliderStyle, RelativeSource={RelativeSource AncestorType=UserControl}}"
            TickFrequency="1"
            Value="{Binding SliderValue, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}" />
    </Grid>
</UserControl>
